export const API_URL = 'http://localhost:8000/api/images/';
/*
const images = this.state.images.image;
    const listItems = images.map((image) => <li>{image}</li>);
    return (
      <ul>
        {listItems}
      </ul>
    );
    */